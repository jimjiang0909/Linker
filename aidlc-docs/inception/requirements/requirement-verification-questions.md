# 需求澄清问题

## 意图分析
- **用户请求**: 查看项目有哪些逻辑是不合理的，或者不完整的
- **请求类型**: Bug Fix + Enhancement（修复逻辑错误 + 完善不完整功能）
- **范围估计**: 多组件（涉及路由、服务、中间件、WebSocket等多个模块）
- **复杂度估计**: 中等（多个独立问题，每个问题本身不复杂）

---

## 问题 1：修复优先级

已发现16个问题，请确认修复范围：

A) 只修复 🔴 严重问题（5个）— 竞态条件、验证码锁定、登录无锁定、Match单向推荐、推荐去重
B) 修复 🔴 严重 + 🟠 中等问题（11个）— 包括状态管理、举报功能、离线消息等
C) 修复所有问题（16个）— 包括时区统一、sortOrder重排、心跳检测等
D) 自定义选择（请在下方说明具体要修复哪些）

[Answer]: C

---

## 问题 2：举报功能的完善程度

当前举报只是记录了一条系统消息。你希望如何完善？

A) 最小方案：创建独立的 Report 数据表 + 去重检查，但不实现管理员审核流程
B) 标准方案：独立 Report 表 + 去重 + 累计举报自动暂停用户（如被3人举报则 suspended）
C) 完整方案：独立 Report 表 + 管理员审核 API + 用户暂停/解封 + 通知机制
D) 暂不处理举报功能，保持现状

[Answer]: B

---

## 问题 3：离线消息存储

当前离线消息存储在内存中，容器重启会丢失。你希望如何处理？

A) 使用 Redis 存储离线消息（需要在 docker-compose 中添加 Redis 服务）
B) 使用 PostgreSQL 存储离线消息（利用现有数据库，添加新表）
C) 保持内存存储，但在文档中标注为已知限制（MVP阶段可接受）
D) 其他方案（请描述）

[Answer]: B

---

## 问题 4：Match 推荐的双向可见性

当前 userB 看不到自己被推荐的 Match 详情。你希望如何修复？

A) 修改 GET /api/matches/daily 接口，同时查询用户作为 userA 和 userB 的 Match
B) 推荐生成时为双方各创建一条 DailyRecommendation 记录
C) 其他方案（请描述）

[Answer]: B

---

## 问题 5：suspended 状态的实现

数据模型中有 suspended 状态但未使用。你希望如何实现？

A) 在 auth 中间件中添加 suspended 检查，被暂停用户无法访问任何 API
B) 仅在匹配和聊天相关接口中检查，被暂停用户仍可查看自己的资料
C) 暂不实现，等举报功能完善后再处理
D) 其他方案（请描述）

[Answer]: A
